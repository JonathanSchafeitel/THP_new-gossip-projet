<div style="text-align: center;">

<h2>Index des utilisateurs, il y a actuellement : <%= User.count %> personnes inscrites</h2>


<% @user.each do  |user| %>
 
  <div class="col">
  <div class="card">
  <div class="card-header">
  Utilisateur numero : <%= user.id %>
  </div>
  <div class="card-body">
    <h5 class="card-title">Prénom de utilisateur : <%= user.first_name %></h5>
    <p class="card-text">Nom de famille : <%= user.last_name %></p>
    <p class="card-text">Mail : <%= user.mail %></p>
    <p class="card-text">Age : <%= user.age %> ans</p>
    <p class="card-text">Description : <%= user.description %></p>
    <a href="<%= user_path(user.id) %>" class="btn btn-primary">Voir le profil</a>
    
  </div>
</div>
</div>
 
  <% end %>
</div>